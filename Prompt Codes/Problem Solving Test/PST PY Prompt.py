# Fix all the issues with the code given below, with explanations as comments:

class Employee:
    def __init__(self name, hours, rate):
        self.name == name 
        self.hours == hours 
        self,rate == rate 

    def calculate_salary(self)
        if self.hours > 40:
            overtime = (self.hours - 40) * self.rate * 1.5
            salary = 40 * self.rate + overtimee
        else:
            salary = hours / rate 
        return salary

    def display(self):
        print("Employee: " + self.name)
        print("Total Salary: Rs." + self.calculate_salary())

employees = [
    Employee("Alice", 45, "300"),
    Employee("Bob", 38, 250),
    Employee("Charlie", 42, 280.5)
]

for emp in employeez:
    emp.display()
